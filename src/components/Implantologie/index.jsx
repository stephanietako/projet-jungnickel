import SectionComponent from "../SectionComponent/SectionComponent";

const Implantologie = ({ title, subtext, text, textmobile, image, alt }) => {
  return (
    <SectionComponent
      title={title}
      subtext={subtext}
      text={text}
      textmobile={textmobile}
      image={image}
      alt={alt}
    />
  );
};

export default Implantologie;
